import React from 'react'
import { withEmployees } from './context/EmployeeProvider'

function EmployeeForm(props) {
    const {handlechange, handleSubmit, createEmployee, inputs  } = props
    return (
        <form>
            <input>
            </input>

        </form>
    )
}